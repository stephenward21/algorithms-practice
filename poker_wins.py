



def check_win(player1hand, player2hand):


	Each hand runs through the check functions. The first successful function appends value. 


	var player1hand_integer_array

	def check_royal_flush(hand):
		suit = hand[0][1]
		face_value_string = "TJQKA"
		for i in range (0, len(hand)):h
			if hand[i][1] != suit
				return False
			face_value_string = face_value_string + hand[i][0]
		hand.append(6)
		return True

		face_value_string = [ 'T', "Q", "K", "J", "A" ]
		array = [ 'TD', 'JD', 'QD', 'KD', 'AD']

		for i in range(array):
			for j in range(face_value_string):
				if array[i][0] = face_value_string[j]:
					face_value_string.remove(face_value_string[j]);
				else:


					return True


	def check_straight_flush(hand):
		for i in range (0, )


	def check_four_kind(hand):


	def check_flush(hand):
		suit = hand[0][1]
		for i in range (0, len(hand)):
			if hand[i][1] != suit
				return False
		hand.append(6)
		return True


	def check_straight(hand):

	def check_fullhouse(hand):

	def check_three_of_kind(hand):


	def check_two_pairs(hand):

	def check_pair(hand):


	def check_high_card(hand):





	def compare_cards
